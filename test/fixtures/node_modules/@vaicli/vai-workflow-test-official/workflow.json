{
  "name": "test-official",
  "description": "An official test workflow",
  "version": "1.0.0",
  "inputs": {
    "query": { "type": "string", "required": true }
  },
  "steps": [
    {
      "id": "search",
      "tool": "query",
      "name": "Search",
      "inputs": { "query": "{{ inputs.query }}" }
    }
  ],
  "output": "{{ search.output }}"
}
