---
phase: 29-phase-26-tech-debt-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/explanations.js
  - docs/commands/chat.mdx
autonomous: true
requirements: [SESS-01, SESS-04]
gap_closure: true

must_haves:
  truths:
    - "vai explain sessions shows all 4 lifecycle states including INITIALIZING"
    - "chat.mdx slash commands table lists all real commands and does not include phantom /stats"
    - "User looking up slash commands in docs finds accurate, complete information"
  artifacts:
    - path: "src/lib/explanations.js"
      provides: "sessions explain topic with 4 lifecycle states"
      contains: "INITIALIZING"
    - path: "docs/commands/chat.mdx"
      provides: "accurate slash commands table"
      contains: "/sources"
  key_links:
    - from: "src/lib/explanations.js"
      to: "src/lib/session-store.js"
      via: "lifecycle states match SESSION_STATES enum"
      pattern: "INITIALIZING.*ACTIVE.*PAUSED.*ARCHIVED"
    - from: "docs/commands/chat.mdx"
      to: "src/commands/chat.js"
      via: "slash commands match switch cases in handleSlashCommand"
      pattern: "/sources|/session|/context|/model|/sessions|/archive|/export"
---

<objective>
Fix two content accuracy issues in Phase 26 deliverables identified by milestone audit:
1. The `sessions` explain topic omits the INITIALIZING lifecycle state (says "three states" but there are four)
2. The `chat.mdx` slash commands table lists phantom `/stats` and omits 7 real commands

Purpose: Close SESS-01 and SESS-04 gaps so CLI help and docs match actual implementation
Output: Corrected explanations.js and chat.mdx
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/26-session-memory-guides/26-01-SUMMARY.md
@.planning/phases/26-session-memory-guides/26-02-SUMMARY.md

<interfaces>
<!-- Source of truth for lifecycle states -->
From src/lib/session-store.js:
```javascript
const SESSION_STATES = Object.freeze({
  INITIALIZING: 'initializing',
  ACTIVE: 'active',
  PAUSED: 'paused',
  ARCHIVED: 'archived',
});

const LIFECYCLE_TRANSITIONS = new Map([
  [SESSION_STATES.INITIALIZING, new Set([SESSION_STATES.ACTIVE])],
  [SESSION_STATES.ACTIVE, new Set([SESSION_STATES.PAUSED, SESSION_STATES.ARCHIVED])],
  [SESSION_STATES.PAUSED, new Set([SESSION_STATES.ACTIVE, SESSION_STATES.ARCHIVED])],
]);
```

<!-- Source of truth for slash commands (from src/commands/chat.js handleSlashCommand switch) -->
Real slash commands:
- /help - Show available slash commands
- /sources - Show sources from last response
- /session - Display current session ID
- /context - Show retrieved context from last query
- /history - List recent chat sessions
- /clear - Clear conversation history
- /model (alias: /models) - Show or switch LLM model
- /memory - Show memory strategy and utilization
- /sessions - List recent sessions
- /archive - Archive current session
- /export [format] [file] - Export conversation (markdown, json, pdf)
- /tools - Show tool calls from last response (agent mode only)
- /export-workflow - Export last tool sequence as workflow (agent mode only)
- /quit (aliases: /exit, /q) - Exit chat
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sessions explain topic lifecycle states</name>
  <files>src/lib/explanations.js</files>
  <action>
In src/lib/explanations.js, find the sessions explain topic (around line 1701). Change "three states" to "four states" and add the INITIALIZING state before ACTIVE in the lifecycle listing.

Current (wrong):
```
`Sessions move through three states:`,
`  ${pc.cyan('ACTIVE')}   → Currently in use or recently used`,
`  ${pc.cyan('PAUSED')}   → Exited normally, can be resumed`,
`  ${pc.cyan('ARCHIVED')} → Closed and summarized for cross-session recall`,
```

Change to:
```
`Sessions move through four states:`,
`  ${pc.cyan('INITIALIZING')} → Session created, waiting for first turn`,
`  ${pc.cyan('ACTIVE')}       → Conversation in progress`,
`  ${pc.cyan('PAUSED')}       → Exited normally, can be resumed`,
`  ${pc.cyan('ARCHIVED')}     → Closed and summarized for cross-session recall`,
```

Keep the surrounding text and structure unchanged. The arrow alignment should use spaces to align the arrow characters visually.
  </action>
  <verify>
    <automated>node -e "const e = require('./src/lib/explanations.js'); const t = e.topics.sessions.content; if (!t.includes('four states')) { process.exit(1); } if (!t.includes('INITIALIZING')) { process.exit(1); } console.log('PASS: sessions topic has 4 states including INITIALIZING');"</automated>
  </verify>
  <done>The sessions explain topic documents all 4 lifecycle states (INITIALIZING, ACTIVE, PAUSED, ARCHIVED) matching the SESSION_STATES enum in session-store.js</done>
</task>

<task type="auto">
  <name>Task 2: Fix chat.mdx slash commands table</name>
  <files>docs/commands/chat.mdx</files>
  <action>
In docs/commands/chat.mdx, replace the current "Slash Commands" section (lines 85-97) with a complete and accurate table. Remove phantom `/stats`. Add all real commands.

Replace the current table with:

```markdown
## Slash Commands

These commands are available at the chat prompt during an active session:

| Command | Description |
|---------|-------------|
| `/help` | Show available slash commands |
| `/sources` | Show sources from last response |
| `/session` | Display current session ID |
| `/context` | Show retrieved context from last query |
| `/history` | List recent chat sessions with status, turn count, and date |
| `/clear` | Clear conversation history for the current session |
| `/model [name]` | Show current LLM model, or switch to `name` |
| `/memory` | Show memory strategy, token budget breakdown, and utilization |
| `/sessions` | List recent sessions |
| `/archive` | Archive the current session |
| `/export [format] [file]` | Export conversation as markdown, json, or pdf |
| `/tools` | Show tool calls from last response (agent mode only) |
| `/export-workflow` | Export last tool sequence as reusable workflow (agent mode only) |
| `/quit` | Exit the chat (aliases: `/exit`, `/q`) |
```

Keep the `/memory Output Example` subsection that follows the table unchanged.
Remove the `/stats` row entirely -- it does not exist in the source code.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('docs/commands/chat.mdx', 'utf8'); const errors = []; if (c.includes('/stats')) errors.push('phantom /stats still present'); ['/sources','/session','/context','/model','/sessions','/archive','/export'].forEach(cmd => { if (!c.includes(cmd)) errors.push('missing ' + cmd); }); if (errors.length) { console.error('FAIL:', errors.join(', ')); process.exit(1); } console.log('PASS: all real slash commands present, no phantom /stats');"</automated>
  </verify>
  <done>chat.mdx slash commands table lists all 14 real commands (/help, /sources, /session, /context, /history, /clear, /model, /memory, /sessions, /archive, /export, /tools, /export-workflow, /quit) and does not include phantom /stats</done>
</task>

</tasks>

<verification>
1. `node -e "const e = require('./src/lib/explanations.js'); console.log(e.topics.sessions.content);"` -- visually confirm 4 states listed
2. `grep -c '/stats' docs/commands/chat.mdx` -- should return 0
3. `grep -c '/sources\|/session\|/context\|/model\|/sessions\|/archive\|/export' docs/commands/chat.mdx` -- should return 7+
</verification>

<success_criteria>
- vai explain sessions shows INITIALIZING, ACTIVE, PAUSED, ARCHIVED (4 states)
- chat.mdx slash commands table has 14 entries matching src/commands/chat.js handleSlashCommand
- No phantom /stats command in docs
- E2E: user reading chat.mdx finds accurate slash command reference
</success_criteria>

<output>
After completion, create `.planning/phases/29-phase-26-tech-debt-fix/29-01-SUMMARY.md`
</output>
