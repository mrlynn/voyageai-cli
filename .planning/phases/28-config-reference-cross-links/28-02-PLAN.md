---
phase: 28-config-reference-cross-links
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - docs/guides/memory-strategies.mdx
  - docs/guides/chat-sessions.mdx
  - docs/guides/cross-session-recall.mdx
  - docs/playground/chat-tab.mdx
autonomous: true
requirements: [XREF-01]

must_haves:
  truths:
    - "User reading the memory strategies guide sees a link or callout pointing them to `vai explain harness` for deeper architectural detail"
    - "At least one docs guide page links to the `vai explain harness` CLI topic"
    - "The cross-reference links are factually accurate and point to the correct CLI command"
  artifacts:
    - path: "docs/guides/memory-strategies.mdx"
      provides: "Cross-reference to vai explain harness in See Also section"
      contains: "vai explain harness"
    - path: "docs/guides/chat-sessions.mdx"
      provides: "Cross-reference to vai explain harness in See Also section"
      contains: "vai explain harness"
  key_links:
    - from: "docs/guides/memory-strategies.mdx"
      to: "vai explain harness"
      via: "See Also section inline code reference"
      pattern: "vai explain harness"
    - from: "docs/guides/chat-sessions.mdx"
      to: "vai explain harness"
      via: "See Also section inline code reference"
      pattern: "vai explain harness"
---

<objective>
Add `vai explain harness` cross-reference links to the guide pages and playground chat tab reference so users can discover the CLI architectural explanation from the docs site.

Purpose: XREF-01 requires at least one docs page to link to `vai explain harness`. This plan adds the link to multiple relevant pages — memory-strategies, chat-sessions, cross-session-recall, and chat-tab — where a user exploring architecture would naturally look. The `chat.mdx` command reference already has this link (added in Phase 26/29) so this plan targets the guide and playground pages.
Output: Updated See Also sections in 4 MDX pages.
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/commands/chat.mdx — existing model: See Also section already contains `vai explain harness` reference (line 191: `` `vai explain harness` -- interactive explanation of the chat architecture ``)
@docs/guides/memory-strategies.mdx — target file for cross-reference addition
@docs/guides/chat-sessions.mdx — target file for cross-reference addition
@docs/guides/cross-session-recall.mdx — target file for cross-reference addition
@docs/playground/chat-tab.mdx — target file for cross-reference addition

<interfaces>
<!-- Pattern for vai explain cross-reference from chat.mdx See Also section -->

The established cross-reference pattern (verbatim from docs/commands/chat.mdx line 191):
```mdx
- `vai explain harness` -- interactive explanation of the chat architecture
```

<!-- Guide page See Also pattern (to be verified by reading the files) -->
Each guide page has a ## See Also section at the bottom with bullet links.
Add the harness line to this section. Do NOT add a separate section — append to existing See Also.

Also add cross-reference to the new reference pages where relevant:
- memory-strategies.mdx: add link to /docs/reference/vai-json-schema for chat.memoryStrategy config
- chat-sessions.mdx: add link to /docs/reference/environment-variables for MONGODB_URI and VAI_CONFIG_PATH
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vai explain harness cross-references to guide pages</name>
  <files>docs/guides/memory-strategies.mdx, docs/guides/chat-sessions.mdx, docs/guides/cross-session-recall.mdx</files>
  <action>
For each of the three guide pages, read the file first to find the existing ## See Also section, then append the `vai explain harness` line.

**docs/guides/memory-strategies.mdx:**
Read the file. Find ## See Also. Add these lines (if not already present):
```
- `vai explain harness` -- architectural deep-dive into the turn state machine, memory, and session systems
- [.vai.json Schema](/docs/reference/vai-json-schema) -- set memoryStrategy as a project default in .vai.json
```

**docs/guides/chat-sessions.mdx:**
Read the file. Find ## See Also. Add these lines (if not already present):
```
- `vai explain harness` -- architectural deep-dive into the turn state machine, memory, and session systems
- [Environment Variables](/docs/reference/environment-variables) -- MONGODB_URI and VAI_CONFIG_PATH for session storage configuration
```

**docs/guides/cross-session-recall.mdx:**
Read the file. Find ## See Also. Add these lines (if not already present):
```
- `vai explain harness` -- architectural deep-dive into the turn state machine, memory, and session systems
```

IMPORTANT: Read each file before editing. Only add lines that are not already present. Preserve all existing See Also content. Do not reformat the page.
  </action>
  <verify>
    <automated>grep -l "vai explain harness" /Users/michael.lynn/code/voyageai-cli/docs/guides/memory-strategies.mdx /Users/michael.lynn/code/voyageai-cli/docs/guides/chat-sessions.mdx /Users/michael.lynn/code/voyageai-cli/docs/guides/cross-session-recall.mdx | wc -l | xargs -I{} test {} -eq 3 && echo "PASS: all 3 guide pages have harness cross-ref" || echo "FAIL"</automated>
  </verify>
  <done>All three guide pages (memory-strategies, chat-sessions, cross-session-recall) contain `vai explain harness` in their See Also sections. Memory strategies guide also links to /docs/reference/vai-json-schema. Chat sessions guide also links to /docs/reference/environment-variables.</done>
</task>

<task type="auto">
  <name>Task 2: Add vai explain harness cross-reference to playground chat tab page</name>
  <files>docs/playground/chat-tab.mdx</files>
  <action>
Read docs/playground/chat-tab.mdx. Find the ## See Also section at the bottom of the file.

Add this line to the See Also section (if not already present):
```
- `vai explain harness` -- architectural deep-dive into the turn state machine, memory, and session systems
```

This gives users who discover the memory bar or turn state indicator UI in the playground a path to understand the underlying architecture via the CLI explain system.

IMPORTANT: Read the file first. Only append if not already present. Do not reformat or restructure the page.
  </action>
  <verify>
    <automated>grep -q "vai explain harness" /Users/michael.lynn/code/voyageai-cli/docs/playground/chat-tab.mdx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>docs/playground/chat-tab.mdx See Also section contains `vai explain harness` cross-reference link.</done>
</task>

</tasks>

<verification>
1. `grep -rl "vai explain harness" docs/guides/` returns all three guide files
2. `grep -q "vai explain harness" docs/playground/chat-tab.mdx` passes
3. `grep -q "vai explain harness" docs/commands/chat.mdx` still passes (pre-existing, not regressed)
4. Total pages with harness cross-reference: docs/commands/chat.mdx (pre-existing) + 3 guides + chat-tab.mdx = 5 pages
5. XREF-01 satisfied: docs site has multiple cross-reference links to `vai explain harness`
</verification>

<success_criteria>
- At least 4 guide/reference pages (memory-strategies, chat-sessions, cross-session-recall, chat-tab) contain `vai explain harness` in their See Also sections
- All cross-references use the consistent format: `` `vai explain harness` -- architectural deep-dive into the turn state machine, memory, and session systems ``
- No existing See Also links were removed or reformatted
- XREF-01 requirement is fully satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/28-config-reference-cross-links/28-02-SUMMARY.md`
</output>
