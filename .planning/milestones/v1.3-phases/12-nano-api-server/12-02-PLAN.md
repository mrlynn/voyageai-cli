---
phase: 12-nano-api-server
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - test/lib/playground-nano-api.test.js
autonomous: true
requirements:
  - ENDP-01
  - ENDP-02
  - ENDP-03
  - ENDP-04

must_haves:
  truths:
    - "Status endpoint returns component readiness without needing nano installed"
    - "Embed endpoint validates input and returns proper error codes on bad input"
    - "Similarity endpoint computes correct NxN matrix from embeddings"
    - "Dimensions endpoint returns embeddings at all 4 MRL levels"
    - "All non-status endpoints return 503 when nano is not ready"
  artifacts:
    - path: "test/lib/playground-nano-api.test.js"
      provides: "Unit tests for all 4 nano API endpoints"
      min_lines: 100
  key_links:
    - from: "test/lib/playground-nano-api.test.js"
      to: "src/lib/playground-nano-api.js"
      via: "direct require and mock context injection"
      pattern: "handleNanoRequest"
---

<objective>
Test all 4 nano API endpoints with mocked dependencies to verify input validation, error handling, readiness checks, and correct response shapes.

Purpose: Ensures the nano API contract is stable before Phase 13+ builds UI against it.
Output: Passing test suite covering all endpoint behaviors.
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-nano-api-server/12-CONTEXT.md
@.planning/phases/12-nano-api-server/12-01-SUMMARY.md

@src/lib/playground-nano-api.js (module under test)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test all 4 nano API endpoints</name>
  <files>test/lib/playground-nano-api.test.js</files>
  <behavior>
    - Status endpoint: returns ready=true when all checks pass, ready=false when any check fails, includes hints only for failing components
    - Embed endpoint: returns 400 when text missing, returns 400 when text exceeds 10000 chars, returns 400 when dimension is invalid (e.g. 999), returns 503 when nano not ready (checkVenv.ok=false), returns embedding with metadata when everything works
    - Similarity endpoint: returns 400 when texts missing, returns 400 when texts has fewer than 2 entries, returns 400 when texts has more than 10 entries, returns 503 when not ready, returns correct NxN matrix shape (e.g. 3x3 for 3 texts), matrix diagonal should be ~1.0
    - Dimensions endpoint: returns 400 when text missing, returns 503 when not ready, returns embeddings at all 4 dimension levels (256, 512, 1024, 2048), includes norm and sparsity stats
    - Unknown /api/nano/ paths: handleNanoRequest returns false
  </behavior>
  <action>
Create `test/lib/playground-nano-api.test.js` using the project's test framework (check existing test files for vitest vs jest vs node:test -- likely node:test based on project patterns).

Build mock helpers:
- `createMockReq(method, url, body)` -- returns a mock IncomingMessage with readable stream behavior for body parsing. Use `Readable.from()` to create a stream from JSON string.
- `createMockRes()` -- returns a mock ServerResponse that captures writeHead status/headers and end() body. Track `statusCode`, `headers`, `body` properties.
- `createMockContext(overrides)` -- returns context object with mock health checks (all ok by default) and mock `generateLocalEmbeddings` that returns fake embeddings of the requested dimension, and mock `readJsonBody` that parses the request body.

For the mock `generateLocalEmbeddings`: return `{ data: [{ embedding: Array(dim).fill(0.1), index: 0 }], model: 'voyage-4-nano', usage: { total_tokens: 5 } }` where dim comes from options.dimensions or 1024.

For mock `readJsonBody`: implement as `async (req) => JSON.parse(await collectBody(req))` where collectBody reads the stream.

Test groups:
1. **GET /api/nano/status** -- test ready=true, test ready=false with hints
2. **POST /api/nano/embed** -- test missing text (400), text too long (400), bad dimension (400), not ready (503), success case with latency
3. **POST /api/nano/similarity** -- test missing texts (400), too few texts (400), too many texts (400), not ready (503), success with 3x3 matrix
4. **POST /api/nano/dimensions** -- test missing text (400), not ready (503), success with all 4 dims
5. **Routing** -- test unknown path returns false

Parse the JSON from `res.body` in assertions. Check status codes via `res.statusCode`.
  </action>
  <verify>
    <automated>npm test -- test/lib/playground-nano-api.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass. Coverage includes: input validation (400s), readiness checks (503s), success paths for all 4 endpoints, correct response shapes, and routing fallthrough.</done>
</task>

</tasks>

<verification>
- `npm test -- test/lib/playground-nano-api.test.js` passes all tests
- Test file covers all 4 endpoints with both success and error cases
</verification>

<success_criteria>
- Test file exists at test/lib/playground-nano-api.test.js
- All tests pass
- Each endpoint has at least: one validation failure test, one readiness failure test (except status), one success test
- Tests use mocked context (no real nano bridge needed)
</success_criteria>

<output>
After completion, create `.planning/phases/12-nano-api-server/12-02-SUMMARY.md`
</output>
