---
phase: 15-cross-bridge-comparison
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - src/playground/index.html
autonomous: false
requirements:
  - XBRIDGE-03

must_haves:
  truths:
    - "Cross-bridge panel visualizes shared embedding space with a clear proof of interoperability"
    - "User sees a visual representation comparing nano and API vector structures"
  artifacts:
    - path: "src/playground/index.html"
      provides: "Vector space visualization in cross-bridge section"
      contains: "nano-xbridge-viz"
  key_links:
    - from: "src/playground/index.html nanoDoCrossbridge()"
      to: "visualization rendering"
      via: "canvas or SVG element update after crossbridge API response"
      pattern: "nano-xbridge-viz"
---

<objective>
Add a visual embedding space proof to the cross-bridge comparison panel that shows users the structural alignment between nano and API vectors.

Purpose: Goes beyond a single similarity number to give users visual confidence that nano and API embeddings are interoperable.
Output: Vector alignment visualization (bar chart comparing vector dimensions side by side) added to the cross-bridge results.
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cross-bridge-comparison/15-01-SUMMARY.md

@src/playground/index.html

<interfaces>
<!-- From 15-01: nanoDoCrossbridge response data structure -->
```javascript
// /api/nano/crossbridge response:
{
  text: "...",
  dimension: 1024,
  nano: { embedding: [...], model: "voyage-4-nano", latency_ms: N },
  api: { embedding: [...], model: "voyage-3-large", latency_ms: N },
  similarity: 0.9987,
  latency_ms: N
}
```

<!-- Existing visualization patterns -->
- buildHeatmap() function exists for vector heatmap rendering (used in embed tab)
- nanoCosineSim(a, b) for client-side cosine similarity
- String concatenation pattern (no template literals)
- CSS variable palette: --accent, --success, --warning, --error, --bg-card, --border
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vector alignment visualization to cross-bridge results</name>
  <files>src/playground/index.html</files>
  <action>
Enhance the nanoDoCrossbridge() function (from 15-01) to render a vector alignment visualization below the similarity score and cards. This provides the "shared embedding space proof" required by XBRIDGE-03.

**CSS additions (in the nano CSS section):**
- `.nano-xbridge-viz` — container (margin-top 16px)
- `.nano-xbridge-viz-title` — section label (font-weight 600, margin-bottom 8px, color var(--accent-text))
- `.nano-xbridge-viz-chart` — chart container (position relative, height 200px, background var(--bg-surface), border-radius var(--radius), border 1px solid var(--border), overflow hidden, padding 8px)
- `.nano-xbridge-viz-bar` — individual dimension bar (position absolute, bottom 0, width determined dynamically)
- `.nano-xbridge-viz-bar.nano-bar` — teal/accent colored (background var(--accent), opacity 0.6)
- `.nano-xbridge-viz-bar.api-bar` — purple colored (background var(--purple), opacity 0.6)
- `.nano-xbridge-viz-legend` — flex row, gap 16px, margin-top 8px, font-size 0.85em
- `.nano-xbridge-viz-legend-item` — flex, align-items center, gap 4px
- `.nano-xbridge-viz-legend-dot` — 10px circle, inline-block
- `.nano-xbridge-diff-stats` — flex row of stat boxes (gap 12px, margin-top 12px)
- `.nano-xbridge-diff-stat` — stat box (background var(--bg-surface), padding 12px, border-radius var(--radius), text-align center, flex 1)
- `.nano-xbridge-diff-stat .stat-value` — font-size 1.4em, font-weight bold, color var(--accent-text)
- `.nano-xbridge-diff-stat .stat-label` — font-size 0.8em, color var(--text-muted), margin-top 4px

**JavaScript (modify nanoDoCrossbridge in the success path, AFTER the existing cards HTML):**

When both nano and api embeddings are available (data.api is not null):

1. **Dimension sample chart:** Sample every Nth dimension (show ~50 bars) from both vectors. For each sampled dimension, render two overlapping bars (nano in teal, API in purple). The chart uses absolutely-positioned divs inside the chart container. Bar height = normalized value mapped to 0-200px range based on min/max across both vectors. This creates a visual "fingerprint" comparison showing the vectors track each other closely.

   Build the chart HTML using string concatenation:
   - Calculate step = Math.max(1, Math.floor(data.nano.embedding.length / 50))
   - Find min/max across sampled values from both vectors
   - For each sampled index i: create two bars side by side
   - Each bar: position left = (barIndex / totalBars) * 100%, height = normalized value as percentage of chart height

2. **Legend:** Below the chart, show a legend with teal dot = "Nano (local)" and purple dot = "API (cloud)"

3. **Diff statistics row:** Three stat boxes:
   - **Max Diff:** Maximum absolute difference across all dimensions: `Math.max(...diffs)` where diffs[i] = Math.abs(nano[i] - api[i]). Format to 6 decimal places.
   - **Avg Diff:** Mean absolute difference across all dimensions. Format to 6 decimal places.
   - **Matching Dims:** Count of dimensions where abs difference < 0.01, displayed as fraction and percentage (e.g., "987/1024 (96.4%)")

Append the visualization + stats HTML after the xbridge-cards div in nanoXbridgeContent.

When data.api is null (no API key), skip the visualization entirely.
  </action>
  <verify>
    <automated>cd /Users/michael.lynn/code/voyageai-cli && node -e "const html = require('fs').readFileSync('src/playground/index.html','utf8'); const checks = ['nano-xbridge-viz','nano-xbridge-diff-stats','nano-bar','api-bar','nano-xbridge-viz-legend','Matching Dims']; const pass = checks.every(c => html.includes(c)); console.log(pass ? 'OK' : 'MISSING: ' + checks.filter(c => !html.includes(c)).join(', '))"</automated>
  </verify>
  <done>Cross-bridge results include a dimension-by-dimension bar chart comparing nano vs API vectors, a legend, and diff statistics (max diff, avg diff, matching dimensions). Visualization only appears when both embeddings are available.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify cross-bridge comparison UI</name>
  <what-built>Complete cross-bridge comparison feature: dual embedding generation, cosine similarity score, side-by-side cards, vector alignment chart, and diff statistics in the Local Inference tab.</what-built>
  <how-to-verify>
    1. Start playground: `vai playground` (or `node src/cli.js playground`)
    2. Navigate to the Local Inference tab
    3. Scroll down past Similarity Matrix and MRL Dimension Comparison to find Cross-Bridge Comparison section
    4. Verify the section appears (requires nano bridge to be ready)
    5. Enter text (or use default) and click "Compare Bridges"
    6. If API key is configured:
       - Verify similarity score appears with color coding
       - Verify two cards show (Nano Local + API Cloud) with model, latency, vector preview
       - Verify bar chart shows overlapping teal/purple bars tracking each other
       - Verify legend shows "Nano (local)" and "API (cloud)"
       - Verify diff stats show Max Diff, Avg Diff, Matching Dims
       - Verify interpretation text appears below score
    7. If API key is NOT configured:
       - Verify helpful message about adding API key appears
    8. Try different dimensions (256, 512, 1024, 2048) to confirm they all work
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Complete cross-bridge comparison flow works end-to-end
2. Vector visualization clearly demonstrates embedding space alignment
3. All three XBRIDGE requirements are met
</verification>

<success_criteria>
- Vector alignment chart visually proves nano and API embeddings share the same space
- Diff statistics provide quantitative proof of interoperability
- Feature degrades gracefully without API key
- All XBRIDGE requirements (01, 02, 03) complete
</success_criteria>

<output>
After completion, create `.planning/phases/15-cross-bridge-comparison/15-02-SUMMARY.md`
</output>
