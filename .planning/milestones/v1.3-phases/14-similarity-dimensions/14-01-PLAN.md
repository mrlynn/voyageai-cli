---
phase: 14-similarity-dimensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/playground/index.html
autonomous: false
requirements: [SIM-01, SIM-02, SIM-03]

must_haves:
  truths:
    - "User can enter 2-10 texts in a multi-line input area for similarity comparison"
    - "User can see an NxN cosine similarity heatmap with color-coded cells"
    - "Heatmap highlights highest and lowest similarity pairs visually"
  artifacts:
    - path: "src/playground/index.html"
      provides: "Nano similarity section HTML, CSS, and JS"
      contains: "nanoDoSimilarity"
  key_links:
    - from: "src/playground/index.html (nanoDoSimilarity)"
      to: "/api/nano/similarity"
      via: "fetch POST with texts array"
      pattern: "fetch.*api/nano/similarity"
---

<objective>
Add an NxN similarity heatmap panel to the Local Inference tab. Users enter 2-10 texts (one per line), click Compare, and see a color-coded cosine similarity matrix with the highest and lowest pairs highlighted.

Purpose: Fulfills SIM-01, SIM-02, SIM-03 -- the similarity matrix requirements for Phase 14.
Output: Working similarity section in the nano tab of the playground.
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-setup-status-embed-ui/13-02-SUMMARY.md

<interfaces>
<!-- API endpoint already exists in src/lib/playground-nano-api.js -->
POST /api/nano/similarity
  Request:  { texts: string[], dimension?: number }
  Response: { texts: string[], matrix: number[][], dimension: number, latency_ms: number }
  Validation: 2-10 texts, each max 10000 chars, dimension in [256, 512, 1024, 2048]

<!-- Existing nano tab patterns from index.html -->
- nanoStatusCache: cached status object (check .ready before allowing operations)
- nanoInit(): fetches /api/nano/status on tab switch
- nanoDoEmbed(): existing pattern for fetch + render in nano tab
- showError(id, msg) / hideError(id): existing error display helpers
- setLoading(btnId, bool): existing button loading state helper
- buildHeatmap(vec, container): existing single-vector heatmap (NOT the NxN matrix -- new rendering needed)
- String concatenation pattern (not template literals) for browser compatibility

<!-- Nano tab panel structure -->
- Tab panel: div#tab-nano
- Embed section: div#nanoEmbedSection (shown when nano ready)
- New similarity section goes AFTER nanoEmbedSection, BEFORE the closing div of tab-nano
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add similarity matrix HTML, CSS, and JS to nano tab</name>
  <files>src/playground/index.html</files>
  <action>
Add a Similarity Matrix section to the nano tab panel (div#tab-nano), placed after the nanoEmbedSection div and before the closing div of tab-nano.

**HTML (inside div#tab-nano, after nanoEmbedSection):**
Add a new div#nanoSimilaritySection with style="display:none;" (same pattern as nanoEmbedSection -- shown only when nano ready). Contents:
1. A card with card-title "Similarity Matrix"
2. A textarea#nanoSimTexts with rows="6" and placeholder text showing "Enter 2-10 texts, one per line..." Pre-populate with 4 example texts about vector search, embeddings, cosine similarity, and RAG.
3. An options-row with a dimension select#nanoSimDimension (256/512/1024/2048, default 1024)
4. A "Compare" button#nanoSimBtn with onclick="nanoDoSimilarity()"
5. An error div.error-msg#nanoSimError
6. A result section div.result-section#nanoSimResult containing:
   - A stats row div#nanoSimStats for latency/dimension info
   - A div#nanoSimHeatmap for the heatmap grid
   - A div#nanoSimHighlights for the highest/lowest pair callouts

**CSS (add in the nano CSS section near .nano-status-grid styles):**
- .nano-sim-heatmap: CSS grid container for the NxN matrix
- .nano-sim-cell: individual cell with min-width 60px, text-align center, border-radius 4px, font-size 0.8em, padding 8px 4px. Color ranges from --error (low similarity, ~0.0) through --warning (mid ~0.5) to --success (high ~1.0) via inline style backgrounds.
- .nano-sim-label: row/column text labels, truncated with ellipsis at 80px max-width, font-size 0.75em, color var(--text-muted)
- .nano-sim-corner: empty top-left corner cell
- .nano-sim-highlights: flex row with gap 16px, margin-top 12px
- .nano-sim-highlight-card: small card showing "Highest" or "Lowest" pair with score, texts, and a colored accent border (--success for highest, --error for lowest)

**JS (add after nanoCopyVector function, before the Home Page section):**

window.nanoDoSimilarity = async function():
1. hideError('nanoSimError')
2. Parse textarea value by splitting on newlines, trim each, filter out empty lines
3. Validate: 2-10 texts, show error if invalid
4. setLoading('nanoSimBtn', true)
5. fetch POST /api/nano/similarity with { texts, dimension }
6. On success, render:
   a. Stats: dimension, text count, latency_ms
   b. Heatmap grid: Build an (N+1) x (N+1) grid where row 0 and col 0 are labels. For each cell (i,j), compute a background color interpolated between red (0.0) -> yellow (0.5) -> green (1.0) using HSL: hue = score * 120, saturation 70%, lightness 35%. Display the score as text with 3 decimal places.
   c. Highlights: Find the highest off-diagonal score and the lowest off-diagonal score. Display each in a highlight card showing the two texts and the score.
7. Show nanoSimResult with .visible class
8. Catch errors -> showError, finally -> setLoading false

Also update the nanoInit() function: after the line that shows nanoEmbedSection (embedSection.style.display = 'block'), add a line to show nanoSimilaritySection. After the line that hides nanoEmbedSection, add a line to hide nanoSimilaritySection. Use the same pattern: get element by ID, set display style.

Use string concatenation (not template literals) for all HTML building, matching the existing nanoDoEmbed pattern.
  </action>
  <verify>
    <automated>cd /Users/michael.lynn/code/voyageai-cli && grep -c "nanoDoSimilarity\|nanoSimTexts\|nanoSimHeatmap\|nano-sim-cell\|nano-sim-highlight" src/playground/index.html | xargs test 5 -le</automated>
  </verify>
  <done>
  - Nano tab has a Similarity Matrix section with textarea, dimension select, and Compare button
  - nanoDoSimilarity() JS function exists and POSTs to /api/nano/similarity
  - NxN heatmap renders with color-coded cells (red-yellow-green gradient)
  - Highest and lowest similarity pairs are displayed in highlight cards
  - Section visibility tied to nano bridge readiness (shown/hidden in nanoInit)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify similarity matrix UI</name>
  <action>Human verifies the similarity matrix UI works correctly in the browser.</action>
  <what-built>NxN similarity heatmap in the Local Inference tab. Enter multiple texts, click Compare, and see a color-coded matrix with highlighted highest/lowest pairs.</what-built>
  <how-to-verify>
    1. Run: cd /Users/michael.lynn/code/voyageai-cli && npm run playground (or node src/cli.js playground)
    2. Open the playground in browser, switch to the Local Inference tab
    3. If nano bridge is ready, scroll past the Embed section to find the Similarity Matrix section
    4. Enter 3-4 different texts (one per line) in the textarea
    5. Click "Compare" and verify:
       - An NxN heatmap grid appears with color-coded cells
       - Diagonal cells show 1.000 (green/high)
       - Off-diagonal cells show varying scores with appropriate colors
       - Below the heatmap, highest and lowest similarity pairs are shown
    6. Try edge cases: exactly 2 texts, 10 texts
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- nanoDoSimilarity function exists and calls /api/nano/similarity
- Heatmap grid renders with N+1 rows/cols (labels + data)
- Color coding: green for high similarity, red for low, yellow for mid
- Highlight cards show highest and lowest off-diagonal pairs
- Section hidden when nano bridge not ready
</verification>

<success_criteria>
- User can enter 2-10 texts and see an NxN cosine similarity heatmap (SIM-01, SIM-02)
- Highest and lowest similarity pairs are visually highlighted (SIM-03)
- Human verification confirms the UI works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/14-similarity-dimensions/14-01-SUMMARY.md`
</output>
