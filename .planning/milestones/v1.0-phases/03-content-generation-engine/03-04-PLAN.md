---
phase: 03-content-generation-engine
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - ~/code/vai-dashboard/src/components/generation/ContentGeneratorForm.tsx
  - ~/code/vai-dashboard/src/components/generation/ContentPreview.tsx
  - ~/code/vai-dashboard/src/app/(app)/generate/page.tsx
autonomous: false
requirements:
  - CGEN-01
  - CGEN-02
  - CGEN-03
  - CGEN-04

must_haves:
  truths:
    - /generate page renders a content generation form with content type selector
    - Content type selector offers blog-post, social-post, code-example, video-script
    - Topic text input is required before form can be submitted
    - Platform dropdown appears conditionally for social-post and video-script types
    - Form submission calls POST /api/generate and displays loading state
    - Generated draft content displays in a preview panel with the full body text
    - Copy-to-clipboard button copies the draft body text
    - Error states display user-friendly error messages
  artifacts:
    - ~/code/vai-dashboard/src/components/generation/ContentGeneratorForm.tsx
    - ~/code/vai-dashboard/src/components/generation/ContentPreview.tsx
    - ~/code/vai-dashboard/src/app/(app)/generate/page.tsx
  key_links:
    - Calls POST /api/generate from Plan 03-03
    - Page accessible at /generate in the app router
---

<objective>
Create the content generation UI — a form to configure and trigger content generation, and a preview panel to view and copy the generated draft.

Purpose: This is the user-facing interface for all 4 content types. The form collects parameters, calls the API, and displays the result with copy-to-clipboard for easy publishing workflow.

Output:
- ContentGeneratorForm component with type/topic/platform inputs
- ContentPreview component with draft display and copy button
- /generate page combining both in a responsive layout
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<existing_patterns>
<!-- Follow existing component patterns from Phase 2 dashboard UI -->
<!-- Use 'use client' for interactive components -->
<!-- Use Tailwind CSS v4 for styling (CSS-based config, no tailwind.config.ts) -->
<!-- KnowledgePageClient wrapper pattern: thin 'use client' wrapper lifts state while keeping page.tsx a server component -->
</existing_patterns>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContentGeneratorForm component</name>
  <files>
    ~/code/vai-dashboard/src/components/generation/ContentGeneratorForm.tsx
  </files>
  <action>
**Create src/components/generation/ContentGeneratorForm.tsx:**

'use client' component that renders:
- Content type selector (dropdown or radio buttons) with options: Blog Post, Social Post, Code Example, Video Script
- Topic text input (required)
- Platform dropdown (conditional — shown only for social-post and video-script):
  - social-post: LinkedIn, Dev.to, Hashnode, Discord, Slack
  - video-script: YouTube, Loom
- Additional instructions textarea (optional, collapsed by default)
- Generate button with loading state (spinner + "Generating..." text)

Props:
```typescript
interface ContentGeneratorFormProps {
  onGenerated: (response: GenerationResponse) => void;
  onError: (error: string) => void;
  isLoading: boolean;
  setIsLoading: (loading: boolean) => void;
}
```

On submit:
1. Set loading state
2. POST to /api/generate with form data
3. On success: call onGenerated(response)
4. On error: call onError(error.message)
5. Clear loading state

Use Tailwind CSS for styling. Form should be clean and functional.
  </action>
  <verify>
    <manual>Visual check: form renders with all fields, platform dropdown appears/hides correctly</manual>
  </verify>
  <done>
    - ContentGeneratorForm renders all input fields
    - Platform dropdown conditionally visible
    - Loading state shows during generation
    - Form validates required fields before submit
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ContentPreview component</name>
  <files>
    ~/code/vai-dashboard/src/components/generation/ContentPreview.tsx
  </files>
  <action>
**Create src/components/generation/ContentPreview.tsx:**

'use client' component that displays:
- Draft title (from response.draft.title)
- Content type badge
- Platform badge (if set)
- Draft body content (rendered as markdown for blog posts, or as plain text with formatting preserved)
- Copy-to-clipboard button that copies draft.body
- Metadata footer: model used, tokens consumed
- Empty state when no draft has been generated yet

Props:
```typescript
interface ContentPreviewProps {
  response: GenerationResponse | null;
  error: string | null;
}
```

Copy-to-clipboard: Use `navigator.clipboard.writeText()` with a "Copied!" confirmation that fades after 2 seconds.

Error state: Display error message in a red-tinted box when error is set.
  </action>
  <verify>
    <manual>Visual check: preview shows generated content, copy button works, error state displays</manual>
  </verify>
  <done>
    - ContentPreview displays draft content with proper formatting
    - Copy-to-clipboard works and shows confirmation
    - Error state displays user-friendly message
    - Empty state shown when no draft exists
  </done>
</task>

<task type="checkpoint">
  <name>Task 3: Create /generate page and verify end-to-end</name>
  <files>
    ~/code/vai-dashboard/src/app/(app)/generate/page.tsx
  </files>
  <action>
**Create src/app/(app)/generate/page.tsx:**

Page layout:
- Page title: "Generate Content"
- Brief description: "Create AI-powered content drafts using vai knowledge"
- Two-column layout on desktop (form left, preview right), stacked on mobile
- ContentGeneratorForm on the left
- ContentPreview on the right
- State management: response, error, isLoading lifted to page-level client wrapper

Follow the KnowledgePageClient pattern from Phase 2:
- page.tsx can be a server component that renders a client wrapper
- Client wrapper manages state and passes props to form and preview components

Add navigation link to /generate in any existing sidebar/nav if one exists.
  </action>
  <verify>
    <manual>
1. Navigate to /generate in the browser
2. Select "Blog Post" content type, enter a topic about vai embeddings
3. Click Generate — loading state appears
4. Draft content appears in preview panel
5. Click Copy — content copied to clipboard
6. Try each content type (social-post, code-example, video-script)
7. Verify platform dropdown appears for social-post and video-script only
8. Submit with empty topic — validation prevents submission
    </manual>
  </verify>
  <done>
    - /generate page renders with form and preview
    - All 4 content types generate successfully
    - Copy-to-clipboard works
    - Platform dropdown conditional visibility works
    - Error handling displays messages correctly
    - Responsive layout works on desktop and mobile
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd ~/code/vai-dashboard && npm run type-check` — exits 0
2. `cd ~/code/vai-dashboard && npm run build` — exits 0
3. Manual: Navigate to /generate, generate content for each type, verify copy works
</verification>

<success_criteria>
1. User can generate blog post drafts via the UI (CGEN-01)
2. User can generate social posts with platform selection (CGEN-02)
3. User can generate code examples (CGEN-03)
4. User can generate video scripts with platform selection (CGEN-04)
5. Copy-to-clipboard enables the copy-paste publishing workflow
6. TypeScript compiles clean, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-generation-engine/03-04-SUMMARY.md` following the template at @/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</output>
