---
phase: 02-knowledge-base
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .npmignore
  - package.json
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03

must_haves:
  truths:
    - "Python source files (nano-bridge.py, requirements.txt) are included in npm tarball via package.json files field"
    - "Python bytecode files (.pyc) and __pycache__ directories are excluded from npm tarball via .npmignore"
    - "scripts/sync-nano-version.js is wired into npm version lifecycle via package.json version script"
    - "Running `npm version patch --no-git-tag-version` updates BRIDGE_VERSION in nano-bridge.py automatically"
  artifacts:
    - path: ".npmignore"
      provides: "Exclusion rules for Python bytecode"
      contains: "*.pyc"
    - path: "package.json"
      provides: "Version script hook for sync-nano-version.js"
      contains: "sync-nano-version"
  key_links:
    - from: "package.json"
      to: "scripts/sync-nano-version.js"
      via: "npm version lifecycle script"
      pattern: "sync-nano-version"
---

<objective>
Configure release packaging so Python source files ship in the npm tarball, bytecode is excluded, and the version sync script runs automatically on `npm version`.

Purpose: REL-01/02/03 ensure that when vai is published to npm, users get the Python bridge files but not compiled bytecode, and version synchronization happens automatically. These are config-only changes touching .npmignore and package.json.

Output:
- Updated .npmignore with Python bytecode exclusions
- Updated package.json with version lifecycle script
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-knowledge-base/02-RESEARCH.md

<interfaces>
<!-- Current package.json files field: -->
```json
{
  "files": ["src/", "README.md", "CHANGELOG.md", "LICENSE"]
}
```
<!-- This already includes "src/" which covers src/nano/nano-bridge.py and src/nano/requirements.txt (REL-01 satisfied by existing config). Verify this is the case. -->

<!-- Current .npmignore (relevant entries): -->
```
electron/
docs/
screenshots/
video/
*.md (except README.md, CHANGELOG.md)
.DS_Store
.env*
.vscode/
.github/
test/
examples/
*.test.js
demo.gif
demo-readme.gif
demo.tape
voyageai-cli.png
voyageai-cli-playground.png
dist-electron/
```

<!-- scripts/sync-nano-version.js exists from Phase 1 -->
<!-- It reads package.json version and updates BRIDGE_VERSION in src/nano/nano-bridge.py -->

<!-- Current package.json scripts: -->
```json
{
  "scripts": {
    "test": "node --test test/**/*.test.js",
    "reset": "node scripts/reset.js",
    "release": "./scripts/release.sh"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Python bytecode exclusions to .npmignore and wire version script</name>
  <files>.npmignore, package.json</files>
  <action>
**Step 1 -- Update .npmignore:**

Read the current .npmignore first. Append the following section at the end:

```
# Python bytecode (REL-02)
*.pyc
__pycache__/
*.pyo
```

Do NOT remove any existing entries. Only append.

**Step 2 -- Update package.json:**

Read the current package.json first. Add a `"version"` script to the scripts object:

```json
{
  "scripts": {
    "version": "node scripts/sync-nano-version.js && git add src/nano/nano-bridge.py"
  }
}
```

The `"version"` script is a npm lifecycle hook that runs AFTER the version field in package.json is updated but BEFORE the version commit is created. This ensures:
1. sync-nano-version.js reads the new version from package.json
2. Updates BRIDGE_VERSION in nano-bridge.py
3. `git add` stages the changed file so it's included in the version commit

Do NOT modify any other scripts. Just add the `"version"` key.

**Step 3 -- Verify REL-01 (Python files in tarball):**

The `"files": ["src/"]` field in package.json already includes all files under src/, which means src/nano/nano-bridge.py and src/nano/requirements.txt will be in the tarball. No changes needed for REL-01, but verify with the automated check below.
  </action>
  <verify>
    <automated>cd /Users/michael.lynn/code/voyageai-cli && npm pack --dry-run 2>&1 | grep -E "nano-bridge|requirements|pyc|pycache" && echo "---" && node -e "const p = require('./package.json'); console.log('version script:', p.scripts.version || 'MISSING')"</automated>
  </verify>
  <done>
    - .npmignore contains *.pyc and __pycache__/ exclusion patterns
    - package.json scripts.version runs sync-nano-version.js and stages nano-bridge.py
    - npm pack --dry-run shows nano-bridge.py and requirements.txt in tarball
    - npm pack --dry-run does NOT show .pyc or __pycache__ files
  </done>
</task>

</tasks>

<verification>
After task completes:
1. `npm pack --dry-run 2>&1 | grep nano-bridge` -- shows src/nano/nano-bridge.py in tarball
2. `npm pack --dry-run 2>&1 | grep requirements` -- shows src/nano/requirements.txt in tarball
3. `npm pack --dry-run 2>&1 | grep -i pyc` -- shows NO .pyc files
4. `node -e "console.log(require('./package.json').scripts.version)"` -- shows sync-nano-version command
5. `node --test test/**/*.test.js` -- existing tests still pass
</verification>

<success_criteria>
1. Python source files (nano-bridge.py, requirements.txt) confirmed in npm tarball
2. Python bytecode (.pyc, __pycache__) excluded from npm tarball
3. Version script hook wired: `npm version` triggers sync-nano-version.js automatically
4. No existing functionality broken
</success_criteria>

<output>
After completion, create `.planning/phases/02-knowledge-base/02-07-SUMMARY.md` following the template at @/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</output>
