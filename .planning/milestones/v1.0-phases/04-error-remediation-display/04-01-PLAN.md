---
phase: 04-error-remediation-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/embed.js
  - src/commands/ingest.js
  - src/commands/pipeline.js
  - src/nano/nano-protocol.js
  - test/nano/nano-protocol.test.js
autonomous: true
requirements:
  - BRDG-04

must_haves:
  truths:
    - "When vai embed --local fails with a nano error, user sees both the error message AND the .fix remediation command"
    - "When vai ingest --local fails with a nano error, user sees the .fix remediation command"
    - "When vai pipeline --local fails with a nano error, user sees the .fix remediation command"
    - "Pipeline EPIPE errors still display their specific EPIPE message (not clobbered)"
    - "validateResponse is no longer exported or defined in nano-protocol.js"
  artifacts:
    - path: "src/commands/embed.js"
      provides: "nano error remediation display in catch block"
      contains: "formatNanoError"
    - path: "src/commands/ingest.js"
      provides: "nano error remediation display in catch block"
      contains: "formatNanoError"
    - path: "src/commands/pipeline.js"
      provides: "nano error remediation display in catch block"
      contains: "formatNanoError"
    - path: "src/nano/nano-protocol.js"
      provides: "protocol helpers without dead validateResponse code"
  key_links:
    - from: "src/commands/embed.js"
      to: "src/nano/nano-errors.js"
      via: "require formatNanoError"
      pattern: "require.*nano-errors"
    - from: "src/commands/ingest.js"
      to: "src/nano/nano-errors.js"
      via: "require formatNanoError"
      pattern: "require.*nano-errors"
    - from: "src/commands/pipeline.js"
      to: "src/nano/nano-errors.js"
      via: "require formatNanoError"
      pattern: "require.*nano-errors"
---

<objective>
Surface the `.fix` remediation property from nano errors in command catch blocks so users see actionable fix commands when `--local` operations fail. Remove dead `validateResponse` code from nano-protocol.js.

Purpose: Closes BRDG-04 -- every failure mode must display an actionable remediation command. Currently the error taxonomy has `.fix` strings but the command catch blocks only show `err.message`.
Output: Three modified command files with nano error formatting, cleaned nano-protocol.js, updated tests.
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-error-remediation-display/04-CONTEXT.md
@.planning/phases/04-error-remediation-display/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/nano/nano-errors.js:
```javascript
function formatNanoError(err) // returns formatted string with err.message + err.fix
function createNanoError(code, ...args) // creates Error with .code, .message, .fix
```

Canonical usage pattern from src/nano/nano-setup.js:297-301:
```javascript
if (err.code && err.fix) {
  console.error(formatNanoError(err));
} else {
  console.error(ui.error(err.message));
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface .fix in command catch blocks and import formatNanoError</name>
  <files>src/commands/embed.js, src/commands/ingest.js, src/commands/pipeline.js</files>
  <action>
In each of the three command files, add the formatNanoError import and update the catch block to check for nano errors before falling back to generic error display.

**embed.js:**
1. Add `const { formatNanoError } = require('../nano/nano-errors.js');` near top imports
2. In catch block (line 276-280), replace `console.error(ui.error(err.message));` with the conditional pattern:
   ```
   if (err.code && err.fix) {
     console.error(formatNanoError(err));
   } else {
     console.error(ui.error(err.message));
   }
   ```

**ingest.js:**
1. Add `const { formatNanoError } = require('../nano/nano-errors.js');` near top imports
2. In catch block (line 428-431), replace `console.error(ui.error(err.message));` with the same conditional pattern

**pipeline.js:**
1. Add `const { formatNanoError } = require('../nano/nano-errors.js');` near top imports
2. In catch block (line 356-365), the EPIPE check must remain first. Change the `else` branch (line 362-363) from:
   ```
   } else {
     console.error(ui.error(err.message));
   }
   ```
   to:
   ```
   } else if (err.code && err.fix) {
     console.error(formatNanoError(err));
   } else {
     console.error(ui.error(err.message));
   }
   ```

Do NOT change telemetry calls, process.exit, finally blocks, or EPIPE handling.
  </action>
  <verify>
    <automated>node -e "const e = require('./src/commands/embed.js'); const i = require('./src/commands/ingest.js'); const p = require('./src/commands/pipeline.js'); console.log('All command files load without error')" && grep -c "formatNanoError" src/commands/embed.js src/commands/ingest.js src/commands/pipeline.js</automated>
  </verify>
  <done>All three command catch blocks check err.code && err.fix before falling back to ui.error. formatNanoError imported in all three files. EPIPE handling in pipeline.js unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Remove dead validateResponse code and clean up tests</name>
  <files>src/nano/nano-protocol.js, test/nano/nano-protocol.test.js</files>
  <action>
**src/nano/nano-protocol.js:**
1. Remove the entire `validateResponse` function (lines 65-78)
2. Remove `validateResponse` from the `module.exports` object (line 85) so exports are: `{ ENVELOPE_TYPES, createRequest, serializeRequest, parseLine }`

**test/nano/nano-protocol.test.js:**
1. Remove `validateResponse` from the destructured require on line 9
2. Remove the entire `describe('validateResponse', ...)` block (lines 99-125)

Run existing test suite to confirm nothing breaks.
  </action>
  <verify>
    <automated>node --test test/nano/nano-protocol.test.js && node --test test/nano/nano-errors.test.js && node -e "const p = require('./src/nano/nano-protocol.js'); if ('validateResponse' in p) { throw new Error('validateResponse still exported'); } console.log('validateResponse removed')"</automated>
  </verify>
  <done>validateResponse function and its export removed from nano-protocol.js. Corresponding test block and import removed from nano-protocol.test.js. All remaining tests pass.</done>
</task>

</tasks>

<verification>
1. `grep -c "formatNanoError" src/commands/embed.js src/commands/ingest.js src/commands/pipeline.js` -- each file shows at least 2 matches (import + usage)
2. `grep "validateResponse" src/nano/nano-protocol.js` -- returns nothing
3. `node --test test/nano/` -- all nano tests pass
4. `grep "err.code && err.fix" src/commands/embed.js src/commands/ingest.js src/commands/pipeline.js` -- each file has the conditional
</verification>

<success_criteria>
- All three command catch blocks display .fix remediation for nano errors via formatNanoError
- Pipeline EPIPE handling preserved (EPIPE check comes before nano error check)
- validateResponse removed from nano-protocol.js (function + export)
- validateResponse tests removed from nano-protocol.test.js
- All existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-error-remediation-display/04-01-SUMMARY.md`
</output>
