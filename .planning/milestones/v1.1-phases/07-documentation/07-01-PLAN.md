---
phase: 07-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - src/lib/explanations.js
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02

must_haves:
  truths:
    - "README contains a 'Local Inference' section with setup, usage, and nano commands"
    - "'vai explain nano' outputs a CLI-focused guide with workflow, architecture, specs, and tryIt commands"
  artifacts:
    - path: "README.md"
      provides: "Local Inference section with TOC entry"
      contains: "## Local Inference"
    - path: "src/lib/explanations.js"
      provides: "Refreshed voyage-4-nano explain entry"
      contains: "vai nano setup"
  key_links:
    - from: "README.md"
      to: "vai nano setup"
      via: "documented commands"
      pattern: "vai nano setup"
    - from: "src/lib/explanations.js"
      to: "vai embed.*--local"
      via: "tryIt commands"
      pattern: "vai embed.*--local"
---

<objective>
Add documentation for the nano local inference workflow to README and explain content.

Purpose: Developers cannot discover or understand the local inference feature because README has no mention of nano and `vai explain nano` shows stale Python-first instructions instead of the CLI workflow.
Output: README.md with "Local Inference" section; src/lib/explanations.js with refreshed voyage-4-nano entry.
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-documentation/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Local Inference" section to README.md</name>
  <files>README.md</files>
  <action>
Add a "Local Inference" subsection to README.md within the "CLI -- Quick Start" section. Place it AFTER the "Models & Benchmarks" section (which ends around line 429) and BEFORE "Benchmarking Your Data" (line 431).

Also add a TOC entry `- [Local Inference](#local-inference)` in the Table of Contents block (around line 86, after "Models & Benchmarks" and before "Benchmarking Your Data").

Section content (use the research-provided template from 07-RESEARCH.md "Code Examples" section):

1. Section header: `### Local Inference`
2. Opening line: "Run embeddings locally with `voyage-4-nano` -- no API key, no network, no cost." Mention shared embedding space with Voyage 4 API models.
3. Prerequisites callout: Python 3.10+ and ~700MB disk space.
4. Setup subsection: `vai nano setup` and `vai nano status` code block.
5. Usage subsection: Show `vai embed "text" --local`, `vai pipeline ./docs/ --local --db myapp --collection knowledge`, `vai ingest --file corpus.jsonl --local --db myapp --collection docs`.
6. Interactive Demo subsection: `vai demo nano` with brief description.
7. Nano Commands table: setup, status, test, info, clear-cache with descriptions.
8. Upgrade Path subsection: Explain shared embedding space compatibility -- no re-vectorization needed.

Do NOT mention `vai demo chat --local` (Phase 8, not yet built). Only document implemented features.

Keep markdown formatting consistent with existing README sections (use `####` for sub-subsections if needed, match code block style).
  </action>
  <verify>
    <automated>grep -c "Local Inference" README.md | grep -q "[1-9]" && grep -c "vai nano setup" README.md | grep -q "[1-9]" && grep "local-inference" README.md | head -1 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>README.md contains a "Local Inference" section with TOC entry, setup/usage/demo/commands/upgrade-path content, and no references to unimplemented features.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite voyage-4-nano explain entry in explanations.js</name>
  <files>src/lib/explanations.js</files>
  <action>
Replace the entire `'voyage-4-nano'` entry (lines 652-694) in src/lib/explanations.js with a refreshed version that reflects the CLI workflow.

Use the research-provided template from 07-RESEARCH.md "Code Examples" section as the basis. Key changes from current content:

1. Update `title` to: `'voyage-4-nano -- Local Inference with the CLI'`
2. Update `summary` to: `'Zero-API-key embeddings via vai nano setup + --local flag'`
3. Rewrite `content` array with these sections (using existing `pc.bold()`, `pc.cyan()`, `pc.dim()` patterns):
   - "What is voyage-4-nano?" -- open-weight, Apache 2.0, 340M params
   - "CLI Workflow:" -- numbered steps: vai nano setup, vai nano status, vai embed "text" --local
   - "Architecture:" -- Node.js spawns Python subprocess (nano-bridge.py), NDJSON over stdio, warm process ~50-200ms vs ~2s cold start
   - "Key specs:" -- Dimensions (256/512/1024/2048 MRL), Quantization (float32/int8/uint8/binary), Context (32K), Model size (~700MB), Venv path (~/.vai/nano-env/)
   - "Shared embedding space:" -- compatible with voyage-4, voyage-4-lite, voyage-4-large, no re-indexing
4. Update `links` to keep HuggingFace link only (remove the blog link that may go stale).
5. Update `tryIt` array to: `vai nano setup`, `vai nano status`, `vai embed "hello world" --local`, `vai demo nano`, `vai explain shared-space`

REMOVE: All references to `pip install sentence-transformers` as primary workflow, "API status" warning about not being available via API, raw Python code examples. The CLI now handles everything.

Keep all content lines under ~76 characters to avoid wrapping in 80-column terminals (accounting for 2-space indent in explain rendering).
  </action>
  <verify>
    <automated>node -e "const e = require('./src/lib/explanations.js'); const n = e['voyage-4-nano']; if (!n) process.exit(1); if (!n.content.includes('vai nano setup')) process.exit(1); if (n.content.includes('pip install')) process.exit(1); if (!n.tryIt.includes('vai demo nano')) process.exit(1); console.log('PASS')"</automated>
  </verify>
  <done>The voyage-4-nano explain entry references CLI workflow (vai nano setup, --local flag), architecture overview, key specs, and tryIt commands. No stale Python-first or "API status" content remains.</done>
</task>

</tasks>

<verification>
1. `grep "Local Inference" README.md` returns the section header and TOC entry
2. `grep "vai nano setup" README.md` returns setup commands in the new section
3. `node -e "const e = require('./src/lib/explanations.js'); console.log(e['voyage-4-nano'].tryIt)"` shows updated tryIt commands including `vai demo nano`
4. `node -e "const e = require('./src/lib/explanations.js'); if (e['voyage-4-nano'].content.includes('pip install')) { throw new Error('stale content') }"` confirms no stale Python references
</verification>

<success_criteria>
- README.md has a "Local Inference" subsection within "CLI -- Quick Start" with TOC entry
- README documents: prerequisites, setup, usage (embed/pipeline/ingest --local), demo, nano commands table, upgrade path
- `vai explain nano` content uses CLI workflow, architecture, specs, and current tryIt commands
- No references to `pip install sentence-transformers` or "API status" warning in explain content
- No references to unimplemented features (vai demo chat --local)
</success_criteria>

<output>
After completion, create `.planning/phases/07-documentation/07-01-SUMMARY.md`
</output>
