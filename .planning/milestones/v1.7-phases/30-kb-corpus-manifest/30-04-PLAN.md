---
phase: 30-kb-corpus-manifest
plan: 04
type: execute
wave: 2
depends_on:
  - 30-01
  - 30-02
  - 30-03
files_modified:
  - src/kb/corpus/manifest.json
  - package.json
autonomous: true
requirements:
  - CORP-02
  - CORP-03

must_haves:
  truths:
    - "manifest.json lists every corpus document with valid checksums"
    - "package.json includes src/kb/ in the files array so corpus ships in npm package"
    - "verify-kb-manifest.js passes all checks (version match, file existence, checksum validity)"
  artifacts:
    - path: "src/kb/corpus/manifest.json"
      provides: "Complete manifest with ~36 document entries, version, checksums"
      contains: "\"version\""
    - path: "package.json"
      provides: "Updated files array including src/ (already covers src/kb/)"
  key_links:
    - from: "src/kb/corpus/manifest.json"
      to: "src/kb/corpus/**/*.md"
      via: "document entries with paths and checksums"
      pattern: "\"path\":.*\\.md"
    - from: "src/kb/corpus/manifest.json"
      to: "package.json"
      via: "version field matches package version"
      pattern: "\"version\""
    - from: "scripts/verify-kb-manifest.js"
      to: "src/kb/corpus/manifest.json"
      via: "reads and validates manifest"
      pattern: "manifest\\.json"
---

<objective>
Generate the manifest.json from authored corpus, verify package bundling, and run the full verification suite.

Purpose: This is the integration step -- the manifest generator produces the real manifest from all ~36 authored docs, and the verification script confirms everything is correct. This completes the phase.
Output: A valid manifest.json and confirmed npm bundling.
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v1.7-phases/30-kb-corpus-manifest/30-CONTEXT.md
@.planning/milestones/v1.7-phases/30-kb-corpus-manifest/30-01-SUMMARY.md
@.planning/milestones/v1.7-phases/30-kb-corpus-manifest/30-02-SUMMARY.md
@.planning/milestones/v1.7-phases/30-kb-corpus-manifest/30-03-SUMMARY.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate manifest and verify npm bundling</name>
  <files>
    src/kb/corpus/manifest.json
    package.json
  </files>
  <action>
1. **Run the manifest generator:**
   ```
   node scripts/generate-kb-manifest.js
   ```
   This should scan all ~36 .md files in src/kb/corpus/, read their YAML front matter, compute SHA-256 checksums, and write src/kb/corpus/manifest.json.

2. **Verify the generated manifest:**
   - Open src/kb/corpus/manifest.json and confirm:
     - `version` matches package.json version
     - `generatedAt` is a valid ISO timestamp
     - `chunkStrategy` is "recursive"
     - `chunkSize` is 512, `chunkOverlap` is 50
     - `embeddingModel` is "voyage-4-large"
     - `documents` array has ~36 entries
     - Each entry has: id, path, url, title, type, section, difficulty, estimatedChunks, checksum

3. **Verify npm package bundling:**
   - Read package.json `files` array. Currently it includes `"src/"` which already covers `src/kb/corpus/`.
   - If `"src/"` is in the files array, no change needed -- corpus is already bundled.
   - If for some reason `src/` is not listed, add `"src/kb/"` to the files array.
   - Run `npm pack --dry-run 2>&1 | grep "kb/corpus"` to confirm corpus files appear in the package listing.

4. **Run the verification script:**
   ```
   node scripts/verify-kb-manifest.js
   ```
   All checks should pass: version match, file existence, no orphans, checksum validity.

5. **Fix any issues found:**
   - If the generator script has bugs (e.g., front matter parsing issues), fix them in scripts/generate-kb-manifest.js and re-run.
   - If any corpus files have malformed front matter, fix those files.
   - Re-run both scripts until verification passes cleanly.
  </action>
  <verify>
    <automated>node scripts/generate-kb-manifest.js && node scripts/verify-kb-manifest.js</automated>
  </verify>
  <done>manifest.json exists with ~36 document entries, all checksums valid, version matches package.json, npm pack includes corpus files, verify script exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Final corpus audit and cleanup</name>
  <files>src/kb/corpus/manifest.json</files>
  <action>
Run a final audit of the complete corpus:

1. **Document count check:**
   ```
   find src/kb/corpus -name "*.md" | wc -l
   ```
   Should be ~36 (20 explainers + 6 guides + 5 reference + 5 examples).

2. **Word count check:**
   ```
   wc -w src/kb/corpus/*/*.md
   ```
   Each file should be roughly 400-600 words. Flag any outliers.

3. **Front matter validation:**
   For every .md file, verify the YAML front matter has all 4 required fields: title, type, section, difficulty. Write a quick inline check:
   ```
   for f in src/kb/corpus/*/*.md; do
     head -10 "$f" | grep -q "title:" || echo "MISSING title: $f"
     head -10 "$f" | grep -q "type:" || echo "MISSING type: $f"
     head -10 "$f" | grep -q "section:" || echo "MISSING section: $f"
     head -10 "$f" | grep -q "difficulty:" || echo "MISSING difficulty: $f"
   done
   ```

4. **Fix any issues found** -- missing front matter, word count outliers, etc.

5. **Re-generate manifest if any files were modified:**
   ```
   node scripts/generate-kb-manifest.js
   node scripts/verify-kb-manifest.js
   ```

6. **Final npm pack check:**
   ```
   npm pack --dry-run 2>&1 | grep -c "kb/corpus"
   ```
   Should show ~37 files (36 .md + 1 manifest.json).
  </action>
  <verify>
    <automated>node scripts/verify-kb-manifest.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All ~36 corpus documents pass front matter validation, word count is within 400-600 range, manifest.json is regenerated and verified, npm pack includes all corpus files.</done>
</task>

</tasks>

<verification>
- `node scripts/verify-kb-manifest.js` exits 0
- `cat src/kb/corpus/manifest.json | node -e "const m=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log(m.documents.length)"` shows ~36
- `npm pack --dry-run 2>&1 | grep "kb/corpus" | wc -l` shows ~37 files included
</verification>

<success_criteria>
- manifest.json exists at src/kb/corpus/manifest.json with ~36 document entries
- manifest version matches package.json version
- All checksums are valid (verify script confirms)
- All corpus files are included in npm pack dry run
- No orphaned files outside manifest
- Verification script exits 0
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.7-phases/30-kb-corpus-manifest/30-04-SUMMARY.md`
</output>
