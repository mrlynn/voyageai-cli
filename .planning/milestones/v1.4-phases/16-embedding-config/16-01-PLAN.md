---
phase: 16-embedding-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/playground/index.html
autonomous: true
requirements:
  - EMBD-01
  - EMBD-02
  - EMBD-03

must_haves:
  truths:
    - "User sees an Embedding dropdown in the KB sidebar Configuration section listing voyage-4-nano, voyage-4-lite, voyage-4, voyage-4-large"
    - "voyage-4-nano shows a green LOCAL pill badge; cloud models show a blue API pill badge"
    - "Each option shows pricing (Free, $0.02, $0.06, $0.12)"
    - "When nano is set up and no API key exists, dropdown auto-defaults to voyage-4-nano"
    - "When a model is unavailable, it appears grayed out with a tooltip explaining why"
    - "Selection persists across page reloads via saveChatSettings/loadChatConfig"
  artifacts:
    - path: "src/playground/index.html"
      provides: "Embedding dropdown UI, badge CSS, auto-default JS, save/load wiring"
      contains: "chatEmbeddingModel"
  key_links:
    - from: "src/playground/index.html (chatEmbeddingModel select)"
      to: "saveChatSettings()"
      via: "change event listener"
      pattern: "chatEmbeddingModel.*addEventListener.*change.*saveChatSettings"
    - from: "src/playground/index.html (loadChatConfig)"
      to: "chatEmbeddingModel select"
      via: "sets .value from config response"
      pattern: "chatEmbeddingModel.*value.*=.*data"
---

<objective>
Add the embedding model dropdown to the playground KB sidebar Configuration section with LOCAL/API badges, pricing, disabled states for unavailable models, and auto-default logic.

Purpose: Users need to see and select their embedding model before it can drive retrieval (Plan 02). This establishes the UI and config persistence.
Output: Embedding dropdown visible in config panel with badges, pricing, availability detection, and persistence.
</objective>

<execution_context>
@/Users/michael.lynn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/michael.lynn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-embedding-config/16-CONTEXT.md

@src/playground/index.html
@src/lib/catalog.js

<interfaces>
<!-- Key code patterns the executor needs -->

From src/playground/index.html (KB sidebar config section ~line 8722-8782):
- Existing rows use: `<div class="kb-config-row">` with `<label class="kb-config-label">` and `<select class="settings-select">`
- LLM Provider: `<select id="chatProvider">`, Model: `<select id="chatModel">`, Chat Mode: `<select id="chatMode">`
- New Embedding row should go after the Model row (line ~8744) and before Database row (line ~8756)

From src/playground/index.html (saveChatSettings ~line 16065-16090):
- Collects all form values into a settings object and POSTs to /api/chat/config
- Must add `embeddingModel: document.getElementById('chatEmbeddingModel').value` to settings object

From src/playground/index.html (loadChatConfig ~line 16102-16126):
- Fetches GET /api/chat/config and populates form fields
- Must set chatEmbeddingModel value from response

From src/playground/index.html (sendChatMessage ~line 16659-16704):
- Reads config values and sends POST /api/chat/message
- Must add embeddingModel to the request body

From src/commands/playground.js (GET /api/chat/config ~line 645-661):
- Returns config object — must include embeddingModel field
- Also needs nano availability info for auto-default logic

From src/commands/playground.js (POST /api/chat/config ~line 1424-1460):
- Saves config to .vai.json — must persist embeddingModel to proj.chat.embeddingModel

From src/lib/catalog.js:
```javascript
const MODEL_CATALOG = [
  { name: 'voyage-4-large', price: '$0.12/1M tokens', pricePerMToken: 0.12, family: 'voyage-4', local: undefined },
  { name: 'voyage-4', price: '$0.06/1M tokens', pricePerMToken: 0.06, family: 'voyage-4' },
  { name: 'voyage-4-lite', price: '$0.02/1M tokens', pricePerMToken: 0.02, family: 'voyage-4' },
  { name: 'voyage-4-nano', price: 'Open-weight (free)', pricePerMToken: 0, local: true, family: 'voyage-4' },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add embedding dropdown HTML, badge CSS, and availability detection</name>
  <files>src/playground/index.html</files>
  <action>
1. **Add CSS for pill badges** (in the existing `<style>` section near other KB config styles):
   - `.embed-badge` base: `display:inline-block; font-size:10px; font-weight:600; padding:1px 6px; border-radius:8px; margin-left:6px; vertical-align:middle; text-transform:uppercase; letter-spacing:0.5px;`
   - `.embed-badge-local`: `background:#dcfce7; color:#166534;` (green LOCAL)
   - `.embed-badge-api`: `background:#dbeafe; color:#1e40af;` (blue API)
   - `.embed-option-price`: `font-size:10px; color:var(--text-muted); margin-left:auto; padding-left:8px;`
   - `.embed-option-disabled`: `opacity:0.45; cursor:not-allowed;`
   - Dark theme variants for badges: `.embed-badge-local` in dark → `background:#14532d; color:#86efac;`, `.embed-badge-api` in dark → `background:#1e3a5f; color:#93c5fd;`

2. **Add Embedding dropdown HTML** in the KB sidebar Configuration section (after the Model row at ~line 8744, before the Database row at ~line 8756). Use a custom dropdown (not native select) to support badges and disabled states inside options:

   Actually, use a native `<select>` with a custom rendering approach: the `<select>` holds the value but we render a visible custom dropdown below it for the badge display. Simpler approach per Claude's discretion: use a native `<select id="chatEmbeddingModel">` with option text that includes the badge indicator and price. The selected display will show a companion badge span next to the select.

   ```html
   <div class="kb-config-row">
     <label class="kb-config-label">Embedding</label>
     <div style="display:flex;align-items:center;gap:4px;flex:1;">
       <select class="settings-select" id="chatEmbeddingModel" style="flex:1;">
         <option value="voyage-4-nano" data-local="true" data-price="Free">voyage-4-nano</option>
         <option value="voyage-4-lite" data-price="$0.02/1M">voyage-4-lite</option>
         <option value="voyage-4" data-price="$0.06/1M">voyage-4</option>
         <option value="voyage-4-large" data-price="$0.12/1M">voyage-4-large</option>
       </select>
       <span class="embed-badge embed-badge-local" id="chatEmbedBadge">LOCAL</span>
     </div>
   </div>
   ```

3. **Add JavaScript function `updateEmbedBadge()`** that reads the selected option and updates the badge span:
   - If selected value is `voyage-4-nano` -> show badge as "LOCAL" with class `embed-badge-local`
   - Otherwise -> show badge as "API" with class `embed-badge-api`
   - Also update disabled state: check nano availability via `/api/nano/status` (already exists) and API key via config. Gray out unavailable options by setting `option.disabled = true` and adding a title attribute ("Run vai nano setup" for nano, "API key required" for cloud models).

4. **Add JavaScript function `initEmbeddingDropdown()`** called during page init:
   - Fetch `/api/nano/status` to check if nano is available (bridge running, model downloaded)
   - Check if Voyage API key exists from the config response (already loaded by `loadChatConfig`)
   - Set disabled state on options accordingly
   - Apply auto-default logic: if nano is set up and no API key -> select voyage-4-nano. If API key exists but no nano -> select voyage-4-large. If both exist -> select voyage-4-nano (favor free/local).
   - Call `updateEmbedBadge()`
   - Attach change listener: `chatEmbeddingModel.addEventListener('change', () => { updateEmbedBadge(); saveChatSettingsDebounced(); })`

5. **Wire into saveChatSettings()** (~line 16065): Add `embeddingModel: document.getElementById('chatEmbeddingModel').value` to the settings object.

6. **Wire into loadChatConfig()** (~line 16102): After loading config, if `data.embeddingModel` or `data.chat?.embeddingModel` exists, set `document.getElementById('chatEmbeddingModel').value` to it. Then call `initEmbeddingDropdown()` (which handles auto-default if no saved value).

7. **Wire into sendChatMessage()** (~line 16659): Add `embeddingModel: document.getElementById('chatEmbeddingModel').value` to the fetch body JSON at line ~16704.

8. **Handle the Rerank toggle**: When embedding model is voyage-4-nano, auto-disable the Rerank toggle (set to inactive with a title tooltip "Reranking disabled with local embeddings"). When switching away from nano, re-enable it.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const html = fs.readFileSync('src/playground/index.html','utf8'); const checks = ['chatEmbeddingModel','embed-badge','updateEmbedBadge','initEmbeddingDropdown','embeddingModel','voyage-4-nano','voyage-4-lite','embed-badge-local','embed-badge-api']; const missing = checks.filter(c => !html.includes(c)); if(missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('All UI elements present');"</automated>
  </verify>
  <done>Embedding dropdown visible in KB sidebar config with LOCAL/API badges, pricing in option text, disabled states for unavailable models, auto-default logic, and selection persists via save/load config.</done>
</task>

<task type="auto">
  <name>Task 2: Wire embedding model into backend config endpoints</name>
  <files>src/commands/playground.js</files>
  <action>
1. **Update GET /api/chat/config** (~line 645-661 in playground.js):
   - Add `embeddingModel` to the response from `proj.chat?.embeddingModel || null`
   - Add `nanoAvailable` field: call `checkVenv()` and `checkModel()` from `src/nano/nano-health.js` and return `true` if both `.ok`
   - Add `hasApiKey` field: check `process.env.VOYAGE_API_KEY || getConfigValue('apiKey')` and return boolean

   This lets the frontend know about nano/API availability without a separate fetch.

2. **Update POST /api/chat/config** (~line 1424-1460 in playground.js):
   - Add `if (parsed.embeddingModel !== undefined) proj.chat.embeddingModel = parsed.embeddingModel;` after the existing chat settings saves (~line 1449).

3. **Update POST /api/chat/message** (~line 1533 in playground.js):
   - Extract `embeddingModel` from the parsed request body (add to destructuring at line 1534)
   - Determine if this is a local model: `const isLocalEmbed = embeddingModel === 'voyage-4-nano';`
   - If `isLocalEmbed`:
     - Validate nano is available (checkVenv + checkModel). If not, return 400 with error: `"voyage-4-nano is not available. Run 'vai nano setup' to install."`
     - Import `generateLocalEmbeddings` from `../nano/nano-local`
     - Pass `embedFn: generateLocalEmbeddings, model: 'voyage-4-nano', dimensions: 1024` in opts to chatTurn
     - Force `rerank: false` (reranking disabled with nano per user decision)
   - If API model selected (`voyage-4-lite`, `voyage-4`, `voyage-4-large`):
     - Validate API key exists. If not, return 400 with error: `"Voyage API key required for ${embeddingModel}. Set VOYAGE_API_KEY or run 'vai config set api-key'."`
     - Pass `model: embeddingModel` in opts to chatTurn
   - If no embeddingModel provided (backward compatibility): use existing behavior (default from catalog)
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const code = fs.readFileSync('src/commands/playground.js','utf8'); const checks = ['embeddingModel','nanoAvailable','isLocalEmbed','generateLocalEmbeddings','nano-local','voyage-4-nano is not available','API key required']; const missing = checks.filter(c => !code.includes(c)); if(missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('Backend wiring complete');"</automated>
  </verify>
  <done>GET /api/chat/config returns embeddingModel + availability info. POST /api/chat/config persists embeddingModel. POST /api/chat/message routes to nano bridge or Voyage API based on selected embedding model with proper validation.</done>
</task>

</tasks>

<verification>
1. Start playground: `vai playground` — open KB sidebar Configuration section
2. Verify Embedding dropdown appears after Model row with 4 options
3. Verify voyage-4-nano shows green LOCAL badge, others show blue API badge
4. Verify unavailable models are grayed out
5. Change selection, reload page — verify selection persists
6. Send a chat message — verify embedding model is included in the request
</verification>

<success_criteria>
- Embedding dropdown renders with all 4 Voyage 4 family models
- LOCAL/API badges display correctly with green/blue colors
- Auto-default selects nano when available and no API key
- Selection saves to .vai.json and reloads on page refresh
- Backend validates model availability before processing
- Nano selection auto-disables reranking
</success_criteria>

<output>
After completion, create `.planning/phases/16-embedding-config/16-01-SUMMARY.md`
</output>
